# Database (Railway PostgreSQL in production)
# Use DATABASE_PUBLIC_URL to match Railway's naming for the public/external URL
# Local: postgresql://user:password@localhost:5432/circus_archive?schema=public
# Railway: Copy from Railway > Postgres > Variables > DATABASE_PUBLIC_URL
DATABASE_PUBLIC_URL="postgresql://user:password@localhost:5432/circus_archive?schema=public"

# Application URL
# Local: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Future: Authentication (V2)
# NEXTAUTH_SECRET=""
# NEXTAUTH_URL=""

# Storage Configuration (V6)
# Options: "local" (dev) or "vercel-blob" (prod)
STORAGE_PROVIDER="local"

# Local Storage (when STORAGE_PROVIDER="local")
# Path where video files will be stored - can be a network path
# Default: ./uploads in project root
LOCAL_STORAGE_PATH="./uploads"
# URL prefix for serving local files (leave as default for local dev)
LOCAL_STORAGE_URL="/api/files"

# Vercel Blob (when STORAGE_PROVIDER="vercel-blob")
# Get this from Vercel Dashboard > Storage > Blob > Connect
# BLOB_READ_WRITE_TOKEN=""
